@model IEnumerable<Ratings.Models.Games>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Games List</h2>
        </div>
        <div class="col-6 text" style="text-align:right">
            <a asp-controller="Games" asp-action="Create" class="btn btn-success">
                <i class="fas fa-plus"></i> Add New Games
            </a>
        </div>
    </div>

    <br /><br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-dark" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Games Name
                    </th>
                    <th>
                        Rating
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (Games games in Model)
                {
                    <tr>
                        <td width="50%">@games.Name</td>
                        <td width="30%">@games.Rating</td>
                        <td class="text-center">
                            <div>
                                <a asp-controller="Games" asp-route-Id="@games.Id" asp-action="Edit" class="btn btn-warning ml-2">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <a asp-controller="Games" asp-route-Id="@games.Id" asp-action="Delete" onclick="return confirm('Are you sure to delete this record?')" class="btn btn-danger mx-2">
                                    <i class="fas fa-trash-alt"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No Games Exist.</p>
    }

</div>